# Params to run developement tests

input               : './tests/data/dev.csv'
steps               : 'prepare_inputs,align_assemblies'
aligner             : 'blat'
blat_fastmap        : true
aggregate_chunk_size: 5000000
tracing             : true
mode                : 'one_to_many'
